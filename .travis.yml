sudo: required
services:
  - docker

before_install:
  - docker build -t marcellmajor/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run marcellmajor/react-test npm test -- --coverage

after_success:
  - docker build -t marcellmajor/multi-client ./client
  - docker build -t marcellmajor/multi-nginx ./nginx
  - docker build -t marcellmajor/multi-server ./server
  - docker build -t marcellmajor/multi-worker ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push marcellmajor/multi-client
  - docker push marcellmajor/multi-nginx
  - docker push marcellmajor/multi-server
  - docker push marcellmajor/multi-worker